# KnowledgeForge Module 1 — production Validation Workbench.
# Simplest stack that works: FastAPI + Jinja + SQLite (stdlib) + PyYAML for
# front matter. Contract validation (jsonschema) and the Gateway client arrive
# in Step 2, not here.
fastapi>=0.110
uvicorn>=0.29
jinja2>=3.1
pyyaml>=6.0
pytest>=8.0
